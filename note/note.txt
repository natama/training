inheritance : (Pewarisan) extend: inherit ke 1 class, implement inherit ke banyak class

class creature{
	public function run(){
		return "bebas lari";
	}
}

class people extends creatture(){
	
}

programing : di execute ke server
scripting : di execute ke client


// hari ke 2
git rm --cached file

untuk komit kompleks
git commit.
	tekan i
	kalau sudah beres : esc -> wq -> enter
git diff untuk cek kodingan yang berubah